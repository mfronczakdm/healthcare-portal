/**
 * Shared variable defaults for query files.
 * Copy values into individual query definitions or override per query.
 */
export const sitecoreQueryDefaults = {
  language: "en",
  site: "rockland",
} as const;
